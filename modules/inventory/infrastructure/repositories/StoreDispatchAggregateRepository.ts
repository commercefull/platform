/**
 * Consolidated Store Dispatch Repository
 *
 * Wraps StoreDispatchRepository as a distinct aggregate-aligned repository.
 *
 * Aggregate: Store Dispatch (dispatches between stores/locations)
 */

export { default } from './StoreDispatchRepository';
