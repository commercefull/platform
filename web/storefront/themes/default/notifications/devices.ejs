<%- include("../partials/header") %>

<main class="min-h-screen bg-gray-50 py-8 px-4 sm:px-6 lg:px-8">
  <div class="max-w-2xl mx-auto">
    <!-- Header -->
    <div class="mb-8">
      <a href="/notifications" class="text-blue-600 hover:text-blue-800 text-sm">&larr; <%= t('storefront:backToNotifications') %></a>
      <h1 class="text-3xl font-bold text-gray-900 mt-2"><%= t('storefront:pushNotificationDevices') %></h1>
      <p class="mt-2 text-sm text-gray-600"><%= t('storefront:manageDevices') %></p>
    </div>

    <% if (typeof successMsg !== 'undefined' && successMsg) { %>
      <div class="mb-4 p-4 bg-green-50 border border-green-200 rounded-lg text-green-800 text-sm"><%= successMsg %></div>
    <% } %>
    <% if (typeof errorMsg !== 'undefined' && errorMsg) { %>
      <div class="mb-4 p-4 bg-red-50 border border-red-200 rounded-lg text-red-800 text-sm"><%= errorMsg %></div>
    <% } %>

    <!-- Register Device Form -->
    <div class="bg-white shadow-lg rounded-xl overflow-hidden mb-6">
      <div class="px-6 py-5 border-b border-gray-200">
        <h2 class="text-lg font-semibold text-gray-900"><%= t('storefront:registerDevice') %></h2>
      </div>
      <form method="POST" action="/notifications/devices" class="px-6 py-5 space-y-4">
        <div>
          <label for="deviceToken" class="block text-sm font-medium text-gray-700 mb-1"><%= t('storefront:deviceToken') %></label>
          <input
            type="text"
            id="deviceToken"
            name="deviceToken"
            required
            placeholder="<%= t('storefront:enterDeviceToken') %>"
            class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
        <div>
          <label for="platform" class="block text-sm font-medium text-gray-700 mb-1"><%= t('storefront:platform') %></label>
          <select
            id="platform"
            name="platform"
            required
            class="w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-blue-500"
          >
            <option value=""><%= t('storefront:selectPlatform') %></option>
            <option value="ios"><%= t('storefront:platformIos') %></option>
            <option value="android"><%= t('storefront:platformAndroid') %></option>
            <option value="web"><%= t('storefront:platformWeb') %></option>
          </select>
        </div>
        <button
          type="submit"
          class="inline-flex items-center px-5 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700 transition-colors"
        >
          <%= t('storefront:registerDeviceBtn') %>
        </button>
      </form>
    </div>

    <!-- Registered Devices List -->
    <div class="bg-white shadow-lg rounded-xl overflow-hidden">
      <div class="px-6 py-5 border-b border-gray-200">
        <h2 class="text-lg font-semibold text-gray-900"><%= t('storefront:registeredDevices') %></h2>
      </div>

      <% if (!devices || devices.length === 0) { %>
        <div class="px-6 py-10 text-center text-gray-500 text-sm">
          <%= t('storefront:noDevicesRegistered') %>
        </div>
      <% } else { %>
        <ul class="divide-y divide-gray-200">
          <% devices.forEach(function(device) { %>
            <li class="px-6 py-4 flex items-center justify-between">
              <div>
                <p class="text-sm font-medium text-gray-900 capitalize"><%= device.platform %></p>
                <p class="text-xs text-gray-500 mt-0.5 font-mono truncate max-w-xs"><%= device.deviceToken %></p>
                <p class="text-xs text-gray-400 mt-0.5"><%= t('storefront:added', { date: new Date(device.createdAt).toLocaleDateString() }) %></p>
              </div>
              <form method="POST" action="/notifications/devices/<%= encodeURIComponent(device.deviceToken) %>/delete">
                <button
                  type="submit"
                  class="text-sm text-red-600 hover:text-red-800 font-medium transition-colors"
                  onclick="return confirm('<%= t('storefront:removeDevice') %>')"
                >
                  <%= t('shared:common.remove') %>
                </button>
              </form>
            </li>
          <% }) %>
        </ul>
      <% } %>
    </div>
  </div>
</main>

<%- include("../partials/footer") %>
