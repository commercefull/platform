<%- include('../partials/header', { title: pageName }) %>

<div class="content-wrapper">
  <div class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1 class="m-0"><i class="fas fa-users-cog me-2"></i><%= pageName %></h1>
        </div>
        <div class="col-sm-6">
          <ol class="breadcrumb float-sm-end">
            <li class="breadcrumb-item"><a href="/admin"><%= t('dashboard.title') %></a></li>
            <li class="breadcrumb-item active"><%= t('users:adminUsers') %></li>
          </ol>
        </div>
      </div>
    </div>
  </div>

  <section class="content">
    <div class="container-fluid">
      <!-- Actions -->
      <div class="row mb-3">
        <div class="col-12">
          <a href="/admin/users/create" class="btn btn-primary">
            <i class="fas fa-plus me-2"></i><%= t('users:createAdminUser') %>
          </a>
          <a href="/admin/roles" class="btn btn-outline-secondary ms-2">
            <i class="fas fa-shield-alt me-2"></i><%= t('users:manageRoles') %>
          </a>
        </div>
      </div>

      <!-- Users Table -->
      <div class="row">
        <div class="col-12">
          <div class="card">
            <div class="card-header">
              <h3 class="card-title"><i class="fas fa-list me-2"></i><%= t('users:adminUsers') %></h3>
              <span class="float-end text-muted"><%= t('shared:common.total') %>: <%= total %></span>
            </div>
            <div class="card-body table-responsive p-0">
              <table class="table table-hover text-nowrap">
                <thead>
                  <tr>
                    <th><%= t('shared:common.name') %></th>
                    <th><%= t('shared:common.email') %></th>
                    <th><%= t('shared:common.role') %></th>
                    <th><%= t('common.status') %></th>
                    <th><%= t('shared:common.lastLogin') %></th>
                    <th><%= t('common.actions') %></th>
                  </tr>
                </thead>
                <tbody>
                  <% (users || []).forEach(function(u) { %>
                  <tr>
                    <td>
                      <strong><%= u.firstName || '' %> <%= u.lastName || '' %></strong>
                      <% if (!u.firstName && !u.lastName) { %>
                        <span class="text-muted"><%= t('users:noName') %></span>
                      <% } %>
                    </td>
                    <td><%= u.email %></td>
                    <td>
                      <% if (u.roleName) { %>
                        <span class="badge bg-primary"><%= u.roleName %></span>
                      <% } else { %>
                        <span class="text-muted"><%= t('users:noRole') %></span>
                      <% } %>
                    </td>
                    <td>
                      <span class="badge bg-<%= u.status === 'active' ? 'success' : u.status === 'suspended' ? 'danger' : 'secondary' %>">
                        <%= u.status %>
                      </span>
                    </td>
                    <td><%= u.lastLoginAt ? new Date(u.lastLoginAt).toLocaleDateString() : t('shared:common.never') %></td>
                    <td>
                      <a href="/admin/users/<%= u.userId %>" class="btn btn-sm btn-outline-primary">
                        <i class="fas fa-eye"></i>
                      </a>
                      <button class="btn btn-sm btn-outline-danger" onclick="deleteUser('<%= u.userId %>')">
                        <i class="fas fa-trash"></i>
                      </button>
                    </td>
                  </tr>
                  <% }); %>
                  <% if ((users || []).length === 0) { %>
                  <tr>
                    <td colspan="6" class="text-center text-muted"><%= t('users:noAdminUsers') %></td>
                  </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
            <% if (totalPages > 1) { %>
            <div class="card-footer">
              <nav>
                <ul class="pagination justify-content-center mb-0">
                  <% for (let i = 1; i <= totalPages; i++) { %>
                  <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                    <a class="page-link" href="?page=<%= i %>&status=<%= filters.status || '' %>&role=<%= filters.role || '' %>">
                      <%= i %>
                    </a>
                  </li>
                  <% } %>
                </ul>
              </nav>
            </div>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>

<script>
  async function deleteUser(userId) {
    if (!confirm('<%= t("users:confirmDelete") %>')) return;

    try {
      const response = await fetch(`/admin/users/${userId}`, { method: 'DELETE' });
      const result = await response.json();
      if (result.success) {
        location.reload();
      } else {
        alert('<%= t("common.error") %>: ' + result.message);
      }
    } catch (error) {
      alert('<%= t("common.error") %>: ' + error.message);
    }
  }
</script>

<%- include('../partials/footer') %>
