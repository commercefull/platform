<!-- Page Header -->
<div class="page-header d-print-none">
  <div class="row g-2 align-items-center">
    <div class="col"><h2 class="page-title"><%= pageName %></h2></div>
  </div>
</div>
<section class="content">
  <div class="container-fluid">
    <%- include("../partials/alerts") %>
    <div class="row">
      <div class="col-12"><div class="card"><div class="card-header"><h3 class="card-title"><%= t('inventory:stockMovementHistory') %></h3></div><div class="card-body p-0"><table class="table table-striped"><thead><tr><th><%= t('shared:common.date') %></th><th><%= t('shared:common.sku') %></th><th><%= t('shared:common.type') %></th><th><%= t('shared:common.quantity') %></th><th><%= t('shared:common.reason') %></th></tr></thead><tbody><% if (history && history.length > 0) { %><% history.forEach(h => { %><tr><td><%= h.createdAt ? new Date(h.createdAt).toLocaleString() : "-" %></td><td><%= h.sku || "-" %></td><td><%= h.type || "-" %></td><td><%= h.quantity || 0 %></td><td><%= h.reason || "-" %></td></tr><% }) %><% } else { %><tr><td colspan="5" class="text-center text-muted py-4"><%= t('inventory:noHistory') %></td></tr><% } %></tbody></table></div></div></div>
    </div>
  </div>
</section>
