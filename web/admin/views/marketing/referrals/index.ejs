<!-- Page Header -->
<div class="page-header d-print-none">
  <div class="row g-2 align-items-center">
    <div class="col">
      <h2 class="page-title"><%= t('marketing:referrals.title') %></h2>
    </div>
  </div>
</div>

<section class="content">
  <div class="container-fluid">
    <%- include('../../partials/alerts') %>

    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= t('marketing:referrals.title') %></h3>
        <span class="float-end text-muted"><%= (referrals || []).length %> <%= t('common.records') %></span>
      </div>
      <div class="card-body table-responsive p-0">
        <table class="table table-hover">
          <thead>
            <tr>
              <th><%= t('shared:common.code') %></th>
              <th><%= t('marketing:referrals.referrerId') %></th>
              <th><%= t('marketing:referrals.referredEmail') %></th>
              <th><%= t('common.status') %></th>
              <th><%= t('marketing:referrals.convertedAt') %></th>
              <th><%= t('common.dateCreated') %></th>
            </tr>
          </thead>
          <tbody>
            <% (referrals || []).forEach(function(r) { %>
            <tr>
              <td><code><%= r.code %></code></td>
              <td><code><%= r.referrerId ? r.referrerId.substring(0, 8) + '...' : '—' %></code></td>
              <td><%= r.referredEmail %></td>
              <td>
                <span class="badge bg-<%= r.status === 'converted' ? 'success' : r.status === 'pending' ? 'warning' : 'secondary' %>">
                  <%= r.status %>
                </span>
              </td>
              <td><%= r.convertedAt ? new Date(r.convertedAt).toLocaleDateString() : '—' %></td>
              <td><%= new Date(r.createdAt).toLocaleDateString() %></td>
            </tr>
            <% }); %>
            <% if ((referrals || []).length === 0) { %>
            <tr><td colspan="6" class="text-center text-muted py-4"><%= t('marketing:referrals.noReferrals') %></td></tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>
