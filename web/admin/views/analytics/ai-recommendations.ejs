<%- include('../partials/header', { title: pageName }) %>

<div class="content-wrapper">
  <div class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1 class="m-0"><i class="fas fa-robot me-2"></i><%= pageName %></h1>
        </div>
        <div class="col-sm-6">
          <ol class="breadcrumb float-sm-end">
            <li class="breadcrumb-item"><a href="/admin/analytics"><%= t('shared:common.analytics') %></a></li>
            <li class="breadcrumb-item active"><%= t('analytics:aiRecommendations') %></li>
          </ol>
        </div>
      </div>
    </div>
  </div>

  <section class="content">
    <div class="container-fluid">
      <!-- Product Recommendations -->
      <div class="row">
        <div class="col-lg-4">
          <div class="card card-primary">
            <div class="card-header">
              <h3 class="card-title"><i class="fas fa-user me-2"></i><%= t('analytics:personalizedRecommendations') %></h3>
            </div>
            <div class="card-body p-0">
              <ul class="list-group list-group-flush">
                <% (productRecommendations.personalized || []).slice(0, 5).forEach(function(rec) { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  <div>
                    <strong><%= t('common.product') %> <%= rec.productId.slice(-8) %></strong>
                    <br><small class="text-muted"><%= rec.reason %></small>
                  </div>
                  <span class="badge bg-primary rounded-pill"><%= (rec.score * 100).toFixed(0) %>%</span>
                </li>
                <% }); %>
                <% if ((productRecommendations.personalized || []).length === 0) { %>
                <li class="list-group-item text-muted text-center"><%= t('analytics:noPersonalizedRecommendations') %></li>
                <% } %>
              </ul>
            </div>
          </div>
        </div>
        <div class="col-lg-4">
          <div class="card card-success">
            <div class="card-header">
              <h3 class="card-title"><i class="fas fa-fire me-2"></i><%= t('analytics:trendingProducts') %></h3>
            </div>
            <div class="card-body p-0">
              <ul class="list-group list-group-flush">
                <% (productRecommendations.trending || []).slice(0, 5).forEach(function(product) { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  <div>
                    <strong><%= t('common.product') %> <%= product.productId.slice(-8) %></strong>
                    <br><small class="text-muted"><%= product.category %></small>
                  </div>
                  <span class="badge bg-success rounded-pill"><i class="fas fa-arrow-up me-1"></i><%= product.trend %></span>
                </li>
                <% }); %>
                <% if ((productRecommendations.trending || []).length === 0) { %>
                <li class="list-group-item text-muted text-center"><%= t('analytics:noTrendingProducts') %></li>
                <% } %>
              </ul>
            </div>
          </div>
        </div>
        <div class="col-lg-4">
          <div class="card card-warning">
            <div class="card-header">
              <h3 class="card-title"><i class="fas fa-link me-2"></i><%= t('analytics:complementaryProducts') %></h3>
            </div>
            <div class="card-body p-0">
              <ul class="list-group list-group-flush">
                <% (productRecommendations.complementary || []).slice(0, 5).forEach(function(product) { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                  <div>
                    <strong><%= t('shared:common.product') %> <%= product.productId.slice(-8) %></strong>
                    <br><small class="text-muted"><%= t('analytics:pairsWith') %> <%= product.baseProductId.slice(-8) %></small>
                  </div>
                  <span class="badge bg-warning text-dark rounded-pill"><%= t('analytics:lift') %>: <%= product.lift %></span>
                </li>
                <% }); %>
                <% if ((productRecommendations.complementary || []).length === 0) { %>
                <li class="list-group-item text-muted text-center"><%= t('analytics:noComplementaryProducts') %></li>
                <% } %>
              </ul>
            </div>
          </div>
        </div>
      </div>

      <!-- Campaign Suggestions -->
      <div class="row">
        <div class="col-12">
          <div class="card">
            <div class="card-header">
              <h3 class="card-title"><i class="fas fa-bullhorn me-2"></i><%= t('analytics:aiCampaignSuggestions') %></h3>
            </div>
            <div class="card-body">
              <div class="row">
                <% (personalizedCampaigns.suggestions || []).forEach(function(campaign) { %>
                <div class="col-md-6">
                  <div class="card card-outline card-info">
                    <div class="card-header">
                      <h5 class="card-title"><%= campaign.name %></h5>
                    </div>
                    <div class="card-body">
                      <p><strong><%= t('analytics:targetAudience') %>:</strong> <%= campaign.targetAudience %></p>
                      <div class="row">
                        <div class="col-6">
                          <div class="info-box bg-light mb-0">
                            <span class="info-box-icon bg-success"><i class="fas fa-chart-line"></i></span>
                            <div class="info-box-content">
                              <span class="info-box-text"><%= t('analytics:expectedROI') %></span>
                              <span class="info-box-number"><%= (campaign.expectedROI * 100).toFixed(0) %>%</span>
                            </div>
                          </div>
                        </div>
                        <div class="col-6">
                          <div class="info-box bg-light mb-0">
                            <span class="info-box-icon bg-info"><i class="fas fa-percentage"></i></span>
                            <div class="info-box-content">
                              <span class="info-box-text"><%= t('analytics:confidence') %></span>
                              <span class="info-box-number"><%= (campaign.confidence * 100).toFixed(0) %>%</span>
                            </div>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div class="card-footer">
                      <button class="btn btn-primary btn-sm"><i class="fas fa-play me-1"></i><%= t('analytics:launchCampaign') %></button>
                      <button class="btn btn-outline-secondary btn-sm"><i class="fas fa-edit me-1"></i><%= t('shared:common.customize') %></button>
                    </div>
                  </div>
                </div>
                <% }); %>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Cross-sell Opportunities -->
      <div class="row">
        <div class="col-12">
          <div class="card">
            <div class="card-header">
              <h3 class="card-title"><i class="fas fa-shopping-cart me-2"></i><%= t('analytics:crossSellOpportunities') %></h3>
            </div>
            <div class="card-body table-responsive p-0">
              <table class="table table-hover text-nowrap">
                <thead>
                  <tr>
                    <th><%= t('shared:common.product') %></th>
                    <th><%= t('analytics:baseProduct') %></th>
                    <th><%= t('analytics:confidence') %></th>
                    <th><%= t('common.actions') %></th>
                  </tr>
                </thead>
                <tbody>
                  <% (crossSellOpportunities.accessoryRecommendations || []).forEach(function(rec) { %>
                  <tr>
                    <td><%= rec.productId.slice(-8) %></td>
                    <td><%= rec.baseProductId.slice(-8) %></td>
                    <td>
                      <div class="progress" style="height: 20px; width: 100px;">
                        <div class="progress-bar bg-success" style="width: <%= (rec.confidence * 100).toFixed(0) %>%">
                          <%= (rec.confidence * 100).toFixed(0) %>%
                        </div>
                      </div>
                    </td>
                    <td>
                      <button class="btn btn-sm btn-outline-primary"><i class="fas fa-plus me-1"></i><%= t('analytics:createBundle') %></button>
                    </td>
                  </tr>
                  <% }); %>
                  <% if ((crossSellOpportunities.accessoryRecommendations || []).length === 0) { %>
                  <tr>
                    <td colspan="4" class="text-center text-muted"><%= t('analytics:noCrossSellOpportunities') %></td>
                  </tr>
                  <% } %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>

<%- include('../partials/footer') %>
