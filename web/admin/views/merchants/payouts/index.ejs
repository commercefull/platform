<div class="page-header d-print-none">
  <div class="row g-2 align-items-center">
    <div class="col">
      <h2 class="page-title"><%= t('merchant:merchants.merchantPayouts') %></h2>
    </div>
  </div>
</div>

<section class="content">
  <div class="container-fluid">
    <%- include('../../../partials/alerts') %>

    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= t('merchant:payouts') %></h3>
        <span class="float-end text-muted"><%= (payouts || []).length %> records</span>
      </div>
      <div class="card-body table-responsive p-0">
        <table class="table table-hover">
          <thead>
            <tr>
              <th><%= t('merchant:merchants.payoutId') %></th>
              <th><%= t('common.amount') %></th>
              <th><%= t('common.currency') %></th>
              <th><%= t('common.status') %></th>
              <th><%= t('shared:common.scheduled') %></th>
              <th><%= t('shared:common.processed') %></th>
              <th><%= t('shared:common.reference') %></th>
              <th><%= t('common.actions') %></th>
            </tr>
          </thead>
          <tbody>
            <% (payouts || []).forEach(function(p) { %>
            <tr>
              <td><code><%= p.merchantPayoutId.substring(0, 8) %>...</code></td>
              <td><strong><%= (p.amount || 0).toFixed(2) %></strong></td>
              <td><%= p.currency %></td>
              <td>
                <span class="badge bg-<%= p.status === 'completed' ? 'success' : p.status === 'failed' ? 'danger' : p.status === 'processing' ? 'info' : 'warning' %>">
                  <%= p.status %>
                </span>
              </td>
              <td><%= p.scheduledAt ? new Date(p.scheduledAt).toLocaleDateString() : '-' %></td>
              <td><%= p.processedAt ? new Date(p.processedAt).toLocaleDateString() : '-' %></td>
              <td><%= p.reference || '-' %></td>
              <td>
                <a href="/admin/operations/merchants/<%= merchantId %>/payouts/<%= p.merchantPayoutId %>" class="btn btn-sm btn-outline-info"><%= t('common.view') %></a>
              </td>
            </tr>
            <% }); %>
            <% if ((payouts || []).length === 0) { %>
            <tr><td colspan="8" class="text-center text-muted py-4"><%= t('merchant:merchants.noPayouts') %></td></tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>
