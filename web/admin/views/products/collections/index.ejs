<%- include('../../partials/header', { title: pageName }) %>

<div class="content-wrapper">
  <div class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1 class="m-0"><i class="fas fa-layer-group me-2"></i><%= pageName %></h1>
        </div>
        <div class="col-sm-6">
          <ol class="breadcrumb float-sm-end">
            <li class="breadcrumb-item"><a href="/admin"><%= t('common.breadcrumbHome') %></a></li>
            <li class="breadcrumb-item"><a href="/admin/products"><%= t('shared:common.products') %></a></li>
            <li class="breadcrumb-item active"><%= t('nav.collections') %></li>
          </ol>
        </div>
      </div>
    </div>
  </div>

  <section class="content">
    <div class="container-fluid">
      <% if (success) { %>
      <div class="alert alert-success alert-dismissible">
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        <i class="fas fa-check-circle me-2"></i><%= success %>
      </div>
      <% } %>

      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
          <h3 class="card-title"><%= t('product:collection.title') %></h3>
          <a href="/admin/products/collections/create" class="btn btn-primary btn-sm">
            <i class="fas fa-plus me-1"></i><%= t('product:collection.new') %>
          </a>
        </div>
        <div class="card-body p-0">
          <% if (collections && collections.length > 0) { %>
          <table class="table table-striped mb-0">
            <thead>
              <tr>
                <th><%= t('common.name') %></th>
                <th><%= t('shared:common.slug') %></th>
                <th><%= t('product:category.position') %></th>
                <th><%= t('common.status') %></th>
                <th><%= t('common.actions') %></th>
              </tr>
            </thead>
            <tbody>
              <% collections.forEach(function(col) { %>
              <tr>
                <td><%= col.name %></td>
                <td><code><%= col.slug %></code></td>
                <td><%= col.position %></td>
                <td>
                  <% if (col.isActive) { %>
                    <span class="badge bg-success"><%= t('common.active') %></span>
                  <% } else { %>
                    <span class="badge bg-secondary"><%= t('common.inactive') %></span>
                  <% } %>
                </td>
                <td>
                  <a href="/admin/products/collections/<%= col.productCollectionId %>/edit" class="btn btn-sm btn-outline-primary"><%= t('common.edit') %></a>
                  <form method="POST" action="/admin/products/collections/<%= col.productCollectionId %>?_method=DELETE" class="d-inline" onsubmit="return confirm('<%= t('product:collection.confirmDelete') %>')">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn btn-sm btn-outline-danger"><%= t('common.delete') %></button>
                  </form>
                </td>
              </tr>
              <% }); %>
            </tbody>
          </table>
          <% } else { %>
          <div class="p-4 text-muted text-center"><%= t('product:collection.noCollections') %> <a href="/admin/products/collections/create"><%= t('product:category.createOne') %></a>.</div>
          <% } %>
        </div>
      </div>
    </div>
  </section>
</div>

<%- include('../../partials/footer') %>
