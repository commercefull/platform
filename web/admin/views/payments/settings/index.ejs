<!-- Page Header -->
<div class="page-header d-print-none">
  <div class="row g-2 align-items-center">
    <div class="col">
      <h2 class="page-title"><%= t('payment:settings') %></h2>
    </div>
  </div>
</div>

<section class="content">
  <div class="container-fluid">
    <%- include('../../partials/alerts') %>

    <div class="card">
      <div class="card-header">
        <h3 class="card-title"><%= t('payment:settings') %></h3>
        <span class="float-end text-muted"><%= (settings || []).length %> <%= t('common.records') %></span>
      </div>
      <div class="card-body table-responsive p-0">
        <table class="table table-hover">
          <thead>
            <tr>
              <th><%= t('shared:common.merchants') %> <%= t('common.id') %></th>
              <th><%= t('payment:provider') %></th>
              <th><%= t('shared:common.enabled') %></th>
              <th><%= t('common.dateCreated') %></th>
              <th><%= t('common.actions') %></th>
            </tr>
          </thead>
          <tbody>
            <% (settings || []).forEach(function(s) { %>
            <tr>
              <td><code><%= s.merchantId %></code></td>
              <td><span class="badge bg-info"><%= s.provider %></span></td>
              <td>
                <span class="badge bg-<%= s.isEnabled ? 'success' : 'secondary' %>">
                  <%= s.isEnabled ? t('shared:common.enabled') : t('shared:common.disabled') %>
                </span>
              </td>
              <td><%= new Date(s.updatedAt).toLocaleDateString() %></td>
              <td>
                <button type="button" class="btn btn-sm btn-outline-primary"
                  data-bs-toggle="modal" data-bs-target="#editModal"
                  data-merchant-id="<%= s.merchantId %>"
                  data-provider="<%= s.provider %>"
                  data-enabled="<%= s.isEnabled %>">
                  <%= t('common.edit') %>
                </button>
              </td>
            </tr>
            <% }); %>
            <% if ((settings || []).length === 0) { %>
            <tr><td colspan="5" class="text-center text-muted py-4"><%= t('payment:noMethods') %></td></tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>

<!-- Edit Modal -->
<div class="modal fade" id="editModal" tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content">
      <form method="POST" id="editForm" action="">
        <div class="modal-header">
          <h5 class="modal-title"><%= t('shared:common.saveChanges') %></h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
        </div>
        <div class="modal-body">
          <div class="mb-3">
            <label class="form-label"><%= t('payment:provider') %></label>
            <input type="text" class="form-control" name="provider" id="editProvider" required>
          </div>
          <div class="mb-3">
            <label class="form-label"><%= t('common.status') %></label>
            <select class="form-select" name="isEnabled" id="editEnabled">
              <option value="true"><%= t('shared:common.enabled') %></option>
              <option value="false"><%= t('shared:common.disabled') %></option>
            </select>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal"><%= t('common.cancel') %></button>
          <button type="submit" class="btn btn-primary"><%= t('common.save') %></button>
        </div>
      </form>
    </div>
  </div>
</div>

<script>
document.getElementById('editModal').addEventListener('show.bs.modal', function(e) {
  const btn = e.relatedTarget;
  const merchantId = btn.getAttribute('data-merchant-id');
  document.getElementById('editForm').action = '/admin/payments/settings/' + merchantId;
  document.getElementById('editProvider').value = btn.getAttribute('data-provider');
  document.getElementById('editEnabled').value = btn.getAttribute('data-enabled');
});
</script>
