<%- include('../../layout') %>

<% content %>
<div class="content-wrapper">
  <!-- Content Header (Page header) -->
  <div class="content-header">
    <div class="container-fluid">
      <div class="row mb-2">
        <div class="col-sm-6">
          <h1 class="m-0"><%= t('shared:common.rates') %></h1>
        </div>
        <div class="col-sm-6">
          <ol class="breadcrumb float-sm-right">
            <li class="breadcrumb-item"><a href="/admin"><%= t('common.breadcrumbHome') %></a></li>
            <li class="breadcrumb-item"><a href="/admin/shipping"><%= t('shared:common.shipping') %></a></li>
            <li class="breadcrumb-item active"><%= t('shared:common.rates') %></li>
          </ol>
        </div>
      </div>
    </div>
  </div>

  <!-- Main content -->
  <section class="content">
    <div class="container-fluid">
      <!-- Success/Error Messages -->
      <% if (success) { %>
        <div class="alert alert-success alert-dismissible">
          <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
          <i class="icon fas fa-check"></i> <%= success %>
        </div>
      <% } %>

      <!-- Action Bar -->
      <div class="row mb-3">
        <div class="col-12">
          <a href="/admin/shipping/rates/create" class="btn btn-primary">
            <i class="fas fa-plus"></i> <%= t('distribution:shipping.createRate') %>
          </a>
          <div class="float-right">
            <span class="badge badge-info"><%= t('common.total') %>: <%= rates.length %></span>
          </div>
        </div>
      </div>

      <!-- Filters -->
      <div class="row mb-3">
        <div class="col-md-12">
          <div class="card">
            <div class="card-body">
              <form method="GET" class="form-inline">
                <div class="form-group mr-3">
                  <label for="zoneId" class="mr-2"><%= t('distribution:shipping.zone') %>:</label>
                  <select name="zoneId" id="zoneId" class="form-control">
                    <option value=""><%= t('distribution:shipping.allZones') %></option>
                    <% zones.forEach(zone => { %>
                      <option value="<%= zone.shippingZoneId %>" <%= filters.zoneId === zone.shippingZoneId ? 'selected' : '' %>>
                        <%= zone.name %>
                      </option>
                    <% }); %>
                  </select>
                </div>
                <div class="form-group mr-3">
                  <label for="methodId" class="mr-2"><%= t('distribution:shipping.method') %>:</label>
                  <select name="methodId" id="methodId" class="form-control">
                    <option value=""><%= t('distribution:shipping.allMethods') %></option>
                    <% methods.forEach(method => { %>
                      <option value="<%= method.shippingMethodId %>" <%= filters.methodId === method.shippingMethodId ? 'selected' : '' %>>
                        <%= method.name %>
                      </option>
                    <% }); %>
                  </select>
                </div>
                <button type="submit" class="btn btn-secondary mr-2">
                  <i class="fas fa-filter"></i> <%= t('common.filter') %>
                </button>
                <a href="/admin/shipping/rates" class="btn btn-outline-secondary">
                  <i class="fas fa-times"></i> <%= t('common.clear') %>
                </a>
              </form>
            </div>
          </div>
        </div>
      </div>

      <!-- Rates Table -->
      <div class="card">
        <div class="card-header">
          <h3 class="card-title"><%= t('shared:common.rates') %></h3>
        </div>
        <div class="card-body table-responsive p-0">
          <table class="table table-hover text-nowrap">
            <thead>
              <tr>
                <th><%= t('common.name') %></th>
                <th><%= t('distribution:shipping.zone') %></th>
                <th><%= t('distribution:shipping.method') %></th>
                <th><%= t('common.type') %></th>
                <th><%= t('distribution:shipping.baseRate') %></th>
                <th><%= t('distribution:shipping.freeThreshold') %></th>
                <th><%= t('common.status') %></th>
                <th><%= t('shared:common.priority') %></th>
                <th><%= t('common.actions') %></th>
              </tr>
            </thead>
            <tbody>
              <% if (rates.length === 0) { %>
                <tr>
                  <td colspan="9" class="text-center py-4">
                    <em><%= t('distribution:shipping.noRates') %> <a href="/admin/shipping/rates/create"><%= t('distribution:shipping.createFirstRate') %></a>.</em>
                  </td>
                </tr>
              <% } else { %>
                <% rates.forEach(rate => { %>
                  <tr>
                    <td>
                      <strong><%= rate.name || 'Unnamed Rate' %></strong>
                      <% if (rate.description) { %>
                        <br><small class="text-muted"><%= rate.description %></small>
                      <% } %>
                    </td>
                    <td>
                      <% const zone = zones.find(z => z.shippingZoneId === rate.shippingZoneId); %>
                      <%= zone ? zone.name : 'Unknown Zone' %>
                    </td>
                    <td>
                      <% const method = methods.find(m => m.shippingMethodId === rate.shippingMethodId); %>
                      <%= method ? method.name : 'Unknown Method' %>
                    </td>
                    <td>
                      <span class="badge badge-secondary"><%= rate.rateType %></span>
                    </td>
                    <td>
                      <%= rate.currency %> <%= parseFloat(rate.baseRate).toFixed(2) %>
                      <% if (rate.perItemRate && rate.perItemRate !== '0') { %>
                        <br><small class="text-muted">+ <%= rate.currency %> <%= parseFloat(rate.perItemRate).toFixed(2) %>/item</small>
                      <% } %>
                    </td>
                    <td>
                      <% if (rate.freeThreshold) { %>
                        <%= rate.currency %> <%= parseFloat(rate.freeThreshold).toFixed(2) %>
                      <% } else { %>
                        <span class="text-muted">-</span>
                      <% } %>
                    </td>
                    <td>
                      <% if (rate.isActive) { %>
                        <span class="badge badge-success"><%= t('common.active') %></span>
                      <% } else { %>
                        <span class="badge badge-secondary"><%= t('common.inactive') %></span>
                      <% } %>
                    </td>
                    <td><%= rate.priority %></td>
                    <td>
                      <a href="/admin/shipping/rates/<%= rate.shippingRateId %>" class="btn btn-sm btn-info">
                        <i class="fas fa-eye"></i>
                      </a>
                      <a href="/admin/shipping/rates/<%= rate.shippingRateId %>/edit" class="btn btn-sm btn-primary">
                        <i class="fas fa-edit"></i>
                      </a>
                      <% if (rate.isActive) { %>
                        <button class="btn btn-sm btn-warning" onclick="deactivateRate('<%= rate.shippingRateId %>')">
                          <i class="fas fa-pause"></i>
                        </button>
                      <% } else { %>
                        <button class="btn btn-sm btn-success" onclick="activateRate('<%= rate.shippingRateId %>')">
                          <i class="fas fa-play"></i>
                        </button>
                      <% } %>
                      <button class="btn btn-sm btn-danger" onclick="deleteRate('<%= rate.shippingRateId %>', '<%= rate.name || \'Shipping Rate\' %>')">
                        <i class="fas fa-trash"></i>
                      </button>
                    </td>
                  </tr>
                <% }); %>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </section>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h4 class="modal-title"><%= t('distribution:shipping.deleteRate') %></h4>
        <button type="button" class="close" data-dismiss="modal">
          <span>&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p><%= t('common.deleteRateConfirm') %> "<strong id="deleteRateName"></strong>"?</p>
        <p class="text-danger"><small><%= t('common.thisActionCannotBeUndone') %></small></p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal"><%= t('common.cancel') %></button>
        <button type="button" class="btn btn-danger" id="confirmDeleteBtn"><%= t('distribution:shipping.deleteRate') %></button>
      </div>
    </div>
  </div>
</div>

<script>
function activateRate(rateId) {
  fetch(`/admin/shipping/rates/${rateId}/activate`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    }
  })
  .then(response => response.json())
  .then(data => {
    if (data.success) {
      location.reload();
    } else {
      alert('<%= t("common.error") %>: ' + data.message);
    }
  })
  .catch(error => {
    alert('<%= t("distribution:shipping.errorActivatingRate") %>');
    console.error('Error:', error);
  });
}

function deactivateRate(rateId) {
  fetch(`/admin/shipping/rates/${rateId}/deactivate`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    }
  })
  .then(response => response.json())
  .then(data => {
    if (data.success) {
      location.reload();
    } else {
      alert('<%= t("common.error") %>: ' + data.message);
    }
  })
  .catch(error => {
    alert('<%= t("distribution:shipping.errorDeactivatingRate") %>');
    console.error('Error:', error);
  });
}

function deleteRate(rateId, rateName) {
  document.getElementById('deleteRateName').textContent = rateName;
  document.getElementById('confirmDeleteBtn').onclick = function() {
    fetch(`/admin/shipping/rates/${rateId}`, {
      method: 'DELETE',
      headers: {
        'Content-Type': 'application/json',
      }
    })
    .then(response => response.json())
    .then(data => {
      if (data.success) {
        location.reload();
      } else {
        alert('<%= t("common.error") %>: ' + data.message);
      }
    })
    .catch(error => {
      alert('<%= t("distribution:shipping.errorDeletingRate") %>');
      console.error('Error:', error);
    });
  };
  $('#deleteModal').modal('show');
}
</script>
<% endcontent %>
