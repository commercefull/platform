<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
  <!-- Left navbar links -->
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
    </li>
    <li class="nav-item d-none d-sm-inline-block">
      <a href="/b2b" class="nav-link">Home</a>
    </li>
  </ul>

  <!-- Right navbar links -->
  <ul class="navbar-nav ml-auto">
    <!-- Cart -->
    <li class="nav-item">
      <a class="nav-link" href="/b2b/cart">
        <i class="fas fa-shopping-cart"></i>
        <span class="badge badge-primary navbar-badge">0</span>
      </a>
    </li>
    <!-- Notifications Dropdown Menu -->
    <li class="nav-item dropdown">
      <a class="nav-link" data-toggle="dropdown" href="#">
        <i class="far fa-bell"></i>
        <span class="badge badge-warning navbar-badge">0</span>
      </a>
      <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
        <span class="dropdown-item dropdown-header">No notifications</span>
      </div>
    </li>
    <!-- User Menu -->
    <li class="nav-item dropdown">
      <a class="nav-link" data-toggle="dropdown" href="#">
        <i class="far fa-user"></i>
        <% if (typeof user !== 'undefined' && user) { %>
          <span class="d-none d-md-inline ml-1"><%= user.name || 'User' %></span>
          <% if (user.role) { %>
            <span class="badge badge-info ml-1"><%= user.role %></span>
          <% } %>
        <% } %>
      </a>
      <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
        <a href="/b2b/account" class="dropdown-item">
          <i class="fas fa-user mr-2"></i> My Account
        </a>
        <div class="dropdown-divider"></div>
        <form action="/b2b/logout" method="POST" class="px-3">
          <button type="submit" class="btn btn-block btn-outline-danger btn-sm">
            <i class="fas fa-sign-out-alt mr-2"></i> Logout
          </button>
        </form>
      </div>
    </li>
  </ul>
</nav>
