/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function (knex) {
  return knex.schema.alterTable('contentType', t => {
    t.foreign('defaultTemplate').references('id').inTable('contentTemplate').onDelete('SET NULL');
  });
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function (knex) {
  return knex.schema.alterTable('contentType', t => {
    t.dropForeign('defaultTemplate');
  });
};
